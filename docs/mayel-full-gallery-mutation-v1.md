# Mayel full gallery mutation control

The integrated Trading writer already revises the complete `PictureDetails.PictureURL` sequence. The append behavior originated in the producer: `ADD_SECONDARY` only represented the tail, legacy manifest assembly appended assets without explicit slot decisions, and the friendly review exposed only main replacement or append. The new full-gallery decision extends the existing ordered manifest and existing outboxes; it creates no worker or poller.

`SAVE_FULL_GALLERY_DECISION` (authenticated operator/OWNER) and `PREPARE_FULL_GALLERY_DECISION_V1` (existing private service transport, exact task/item binding) prepare the same contract. They are decisions under the active delegation, not OWNER approval. Each original image has exactly one disposition and each surviving/new image has an explicit final position. REPLACE_MAIN is position zero. ADD materializes shifts, REPLACE inherits its source order, REMOVE requires proof, and only explicit REORDER permits crossing another retained image. Empty galleries, missing positions, unrepresented originals, repeated assets and silent reorder are rejected. The manifest binds account, listing, generation, full official base, own truth, source evidence, asset hashes, reasons and desired final order.

New media uses the existing visual outbox/executor. All EPS preparation completes before its one claimed complete-gallery mutation. Transformations containing only existing images use the existing content outbox and complete-gallery writer. Recovery and retries share the existing quota authority, claim, idempotency and readback gates. Discovery excludes a matching content intent already handed off. No publisher or Ads gate changes.

The original FULL delegation remains immutable. A private, revocable, append-only scope addendum records the OWNER's IMAGE_REMOVAL authorization. Current automatic removal proof is conservative: a source reference with the same SHA-256 must remain in a KEEP/REORDER slot, proving that necessary visual evidence is retained. Other removal decisions remain unproven until equivalent durable semantic evidence is supported; a request boolean is insufficient. Neither identity-drift assets nor unreviewed product claims gain authority.

Normal asset review adopts Mayel's durable placement decision without OWNER slot selection. A previously chosen hero stays a main replacement. Missing placement evidence never defaults to append. The delegated operator can specify a secondary replacement directly; full-gallery strategies specify insertion/removal/reorder before dispatch. No OWNER approval is introduced.

A gallery decision awaiting current eBay evidence is saved in the existing task before any upstream read. The existing bounded discovery resumes it, without a new worker, timer or poller. Its pending record grants no write authority. Material gallery drift stops it with REQUIRES_ATTENTION. That pending state belongs only to its recorded base generation; it cannot suppress a subsequent valid manifest.

Only exact ordered official readback closes a full-gallery intent. Trading verifies the complete EPS URL sequence; Inventory verifies its complete imageUrls plus the official gallery, without perceptual similarity as a substitute for slot identity. Receipts retain expected and actual order. ORDER_OR_REPLACEMENT_MISMATCH cannot become SYNCED. Unknown commit recovery reads before retry and does not issue another write blindly.

Official contract reviewed on 2026-09-10:
- [ReviseFixedPriceItem](https://developer.ebay.com/devzone/xml/docs/Reference/ebay/ReviseFixedPriceItem.html): complete PictureDetails revision.
- [Inventory image/media handling](https://developer.ebay.com/api-docs/sell/static/inventory/managing-image-media.html): preparation and complete imageUrls.

The physical replacement canary remains separate from software certification. On 2026-09-10 the candidate 366643122092 returned Trading error 518; Browse fallback is insufficient write authority. No gallery mutation was attempted. Resume only after the existing quota gate opens, read the complete official base again, choose a safe approved asset and explicit replacement slot, prepare the manifest, then use the existing scoped runtime once and retain its ordered readback receipt. Closed 1136/1192 incidents are not reused.
