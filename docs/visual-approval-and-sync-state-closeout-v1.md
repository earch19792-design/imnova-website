# Visual approval and sync state closeout

The durable receipt for listing 366582671136 previously said `PENDING_EBAY_SYNC` while the executor was holding it for review. A separate issue was that the executor treated operator quality review as sync authority. The existing outbox now requires an explicit, durable OWNER approval for every asset included in the operation. Neither automatic QA, upload rights, nor global delegation grants this approval.

The normal path is `DRAFT → QA_READY → OWNER_APPROVAL_REQUIRED → APPROVED_FOR_EBAY_SYNC → PENDING_EBAY_SYNC → REVALIDATING → SYNCING → OFFICIAL_READBACK_REQUIRED → SYNCED`. QA readiness comes from the existing normalizer/QA receipt; a saved asset with passing QA is shown as awaiting OWNER approval. Private preparation may resume on the existing worker without quota; it stops before any quota or eBay read until authorized. A durable receipt still allows the iPad to close.

Each of the six creative slots retains source references/digests, original and normalized hashes, automatic/human QA, independent OWNER approval, exact asset generation, sync status, and its deterministic approval idempotency key. An atomic unique index prevents concurrent uploads from taking the same active slot. No source image or proposal is replaced to fill an empty slot.

The OWNER approval endpoint checks the authenticated OWNER role. The service-only RPC additionally matches the durable OWNER identity for the account, locks the task/asset, validates QA and exact generation, and persists only that asset's approval. Existing Phase A quality flags keep their meaning. The outbox can advance only when all bound assets and all assets in its proposed gallery have individual approvals; this preserves the existing atomic gallery write. Uncreated proposals do not need approval, and no approval is inherited by another asset.

Before dispatch the existing runtime checks current official listing/images against the frozen base, SKU/title/economic fields, product/source generation, current manifest, and each asset approval. Authority is checked again after preflight. Task/asset changes during an active sync are blocked; material saved-state drift goes to `REQUIRES_ATTENTION`. Dispatch intent is durable before the external call. A lost response remains `OFFICIAL_READBACK_REQUIRED`; recovery reads first and never blindly repeats the write. `SYNCED` requires official verification. Ads remain disabled.

For 1136: **Mayel → Mejorar listings → select the necklace → Abrir Estación visual · 1136**. In the existing proposal, complete **Comparación humana obligatoria** and press **Aprobar** for quality review. The OWNER then presses **Aprobar esta imagen para sincronizar con eBay** on that image. Alternatively, the existing saved-hero Preview's reviewed checkbox and **Confirmar y enviar cuando eBay esté disponible** explicitly approve that one hero and preserve the other images. Neither control was pressed during closeout.

Validation uses isolated six-asset PostgreSQL fixtures and the actual existing six-image upload/QA pipeline, plus sync-engine tests for quota holds/recovery, revoked approval, stale generations, duplicate claims, lost replies, and readback-only recovery. The real preprod receipt and asset remain unapproved. Physical post-quota eBay synchronization is deliberately pending, and no native iPad hardware certification is claimed.

Database access retains service-only RPC grants and existing RLS, following [Supabase's RLS guidance](https://supabase.com/docs/guides/database/postgres/row-level-security). Security advisors were compared against the existing baseline; unrelated findings are unchanged.
