# Mayel: visual intent, asset status and package consistency

QA submission now requires an explicit use and position. `REPLACE_MAIN` replaces position 0 only; `ADD_SECONDARY` appends at an explicit free position, preserving every current image and its order. Existing approved assets can receive an explicit intention without uploading or regenerating. Both QA and subsequent edits create the same manifest contract. Replays preserve that contract. Editing an asset's action/position invalidates its previous OWNER approval atomically; it does not alter other assets or historical receipts.

QA is not write authority. Each asset still requires OWNER approval, followed by the existing complete-gallery confirmation and idempotent durable outbox. The executor checks that confirmation against the current manifest for both replacement and addition. No new scheduler, worker, eBay client call or publication route was added.

The 1192 draft was corrected through the runtime in preprod using OWNER authority without changing its assigned operator. It selects the existing asset `2ee8fe53-fd42-4c4a-ae43-c8b6d908b1f7` as hero with `REPLACE_POSITION_0_ONLY`. OWNER approval remains null. This old task has one saved base image without certified full-gallery authority: its preview does not claim a current LIVE gallery and cannot bypass the full-gallery/revalidation gates. No eBay request or write was made. The previously certified 1136 receipt remains SYNCED with official readback.

Each asset displays a separate icon, label, color and four-step progress indicator. Generated, QA approved, durable saved, awaiting OWNER, awaiting eBay and officially synchronized are distinct. Only a scoped official readback permits the final green state; sending a request or saving a draft never does.

The listing pipeline gate audits the existing Sell One Like This generation against scoped current product, account, supplier SKU, variant, category, required specifics, V2.1, authorized media, price/cost and durable shipping/fee/policy evidence. It does not recreate package content. It returns a deeply frozen, hashed snapshot with source references and a stable publication preparation key. That key is a preparation identity, not the publisher's authorization or execution key, and it grants no write authority. Existing durable publisher immutability/approval guards remain unchanged.

Package consistency and publication readiness are independent. Stale shipping is not used as current; missing shipping, fees, quantitative stock or policy evidence remains pending. The normal reference read reevaluates the same package when fresh evidence arrives, with the existing post-shipping continuation responsible for commercial reevaluation. Neither this gate nor its UI initiates capture. Full publication preflight and OWNER authorization remain necessary.

Evidence is saved in `/home/earch/seller-os-evidence/visual-listing-consolidated-v1/`. Tests cover explicit hero/secondary intent, other-slot preservation, approval/readback gates, independent visual statuses, package binding, immutable hashes, stale evidence, no competitor contamination and later shipping attachment without recreating the package.
